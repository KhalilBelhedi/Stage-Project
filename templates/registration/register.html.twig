

{% block title %}Register{% endblock %}

{% block body %}
<!DOCTYPE html>
<html >
  <head>
   <title>Créer un compte</title>
  <style>
  .div_A
  
  { text-align: left;
    margin : 200px;
  }
      .div_c
      { 
       width: 650px; 
       height: 650px;
       background-color: #D8D9DA;
       border: 5px solid #BB2525; 
       text-align: center;
       position : absolute;
       top : 50%;
       left : 50%;
       transform : translate(-50%,-50%);
       
      }
      body {
        background-color: #ffffff;
        color: #fff;
        font-family: arial;
        font-size: 1.2rem;
      }
        .card {
    background-color: #2c2f3b;
    color: #fff;
    width: 200px;
    margin: auto;
    margin-top: 50px;
    padding: 20px;
    border-radius: 10px;
    text-align: center;
  }

  .form-group {
    margin-bottom: 100px;
  }

  label {
    display: block;
    margin-bottom: 5px;
    text-align: center;
    font-family: Georgia; 
    color: #141E46; 
  }

  input{
    background-color: #3e414c;
    color: #fff;
    border: none;
    border-radius: 5px;
    padding: 10px;
    width: 50%;
    font-size: 1.2rem;
    font-family: Georgia;  
  }

  .bbutton {
    background-color: #141E46;
    color: #fff;
    border: none;
    border-radius: 5px;
    padding: 10px;
    width: 50%;
    text-align: center;
    font-size: 1.2rem;
    cursor: pointer;
    font-family: Georgia;  
  }

  button:hover {
    background-color: #0C356A;
  }

  h1 {
    font-family: Georgia;  
    margin-bottom: 30px;
    color: #141E46 ;
  }
  button {
   
    text-align: right;
    
  }
  img
  {
    left:50px;
    top:50px;
  }
</style>
   

         


 </head>
   <div class="">
   
   
   </div>
   <div class="div_c">
                             <img src="bhPNG.png">
                               <h1>Créer un compte</h1>
                                
    {{ form_errors(registrationForm) }}

    {{ form_start(registrationForm) }}
    {{ form_row(registrationForm.firstname) }}
    {{ form_row(registrationForm.lastname) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
        {{ form_row(registrationForm.agreeTerms) }}

        <button class="bbutton"type="submit" class="btn">S'inscrire</button>
    {{ form_end(registrationForm) }}
  
    <a href="{{ path('app_login') }}" class="nav-item">J'ai deja Un compte</a>
  </div>
                         
{% endblock %}
